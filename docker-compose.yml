version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: code_auditor_redis
    ports:
      - "6380:6379"
    restart: unless-stopped
    command: redis-server --save 60 1 --loglevel warning